import styles from './App.module.css';
import AppContent from './AppContent';

const App = () => {
  return (
    <main className={styles.app}>
      <AppContent />
    </main>
  );
};

export default App;
